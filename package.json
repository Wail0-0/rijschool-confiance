{
  "name": "rijschool-confiance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Rijschool Confiance - landingspagina (React via CDN + Babel in de browser) met Node-server voor het aanmeldformulier.",
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "start": "node server.js",
    "dev": "live-server --port=8000 --entry-file=index.html"
  },
  "dependencies": {
    "express": "^4.21.2",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "live-server": "^1.2.2"
  }
}
